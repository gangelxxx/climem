# Журнал сессий

Срез каждой рабочей сессии: что сделано и **почему**, что осталось. Не пересказ диффа (он в
git) — решения и контекст. Новые записи — сверху, дата абсолютная.

---

## 2026-06-18 — Граф знаний: реализован план P0–P2 (баги + дизайн + доки)

**Что.** Реализован весь приоритизированный план из [../docs/graph-analysis.md](../docs/graph-analysis.md)
§4. Баги: **B1** (висячее ребро не оживало без `--all`) — `store::dangling_edge_sources()` +
ревайвл-проход в инкрементальном `reindex`, перечитывающий md источников; **B2/B3** (входящие
рёбра не чистились → осиротевшие/воскресающие) — `store::dangle_edges_to` (`UPDATE … dst_id=NULL
WHERE dst_id=?`) в `forget` и prune; **B4** (молчаливая коллизия slug) — stderr-warning в
`remember`. Дизайн: **D1/D2** — общее ядро `search::bfs_neighbors` с live-валидацией под оба
обхода; **D3** — хинт при `--related` с `graph=0`; **D4** — дедуп рёбер по `(predicate,
resolved-key)`; **D5** — `graph::normalize_predicate`; **D8/D9** — `config.search.graph_depth`/
`graph_predicate`; **D6/D7** — новая команда `cm backlinks` (`store::edges_to`, ожил индекс
`edges_dst`). Доки **M1–M4** синхронизированы (desc.md §3/§5/§12, README, help.rs). Новые
инварианты (h)/(i)/(j) в [decisions.md](decisions.md); B1–B4 перенесены в «Закрытые»
[known-issues.md](known-issues.md); конвенции графа дополнены [conventions.md](conventions.md).

**Почему важно.** Инкрементальный тракт нарушал заявленные инварианты ((d), (f)): forward-ссылки
молча не резолвились, удаление цели теряло связь из вывода или (хуже) воскрешало её на чужую
заметку при переиспользовании id. Корень — переиндексация только `changed_notes` + односторонняя
синхронизация рёбер (по `src_id`, не `dst_id`). Теперь граф производен от md и в инкрементальном
тракте: висячее оживает даже у неизменившихся источников, удаление ре-дэнглит входящие.

**Как проверял.** `cargo test` — 320 зелёных (новые юнит + e2e регрессии B1/B2/backlinks в
`tests/lifecycle.rs`); `clippy --all-targets` чисто; сборка с фичей `pdf` ок. Плюс **повторно
вживую через `cm.exe`** воспроизвёл сценарии §5: B1 теперь `dangling:false` после
`{"changed":0}`-reindex, B2 даёт `dangling:true` вместо пустоты, B4/D3 печатают warning, backlinks
возвращает источник с предикатом.

**Что осталось.** By-design ограничения (L1 code-span в wikilinks, L2, D10, D11) оставлены и
задокументированы как не-баги. Открытым остаётся только дореформенный баг утечки секрета в
`config get`/`set` (не из этого плана).

---

## 2026-06-18 — папка `memory/` переведена в активный стор climem

**Что сделано.** Сама KB-папка [../memory/](.) стала рабочим хранилищем `cm`: в неё лёг
`cm.exe` (свежая release-сборка), пустой `store.db` + `config.json` (provider `local`, offline)
и собственный `.gitignore` (отсекает `cm.exe`/`store.db`/`models/`). Затем все 19 `.md` дерева
`memory/` (верхний уровень + `howto/`, `rules/`, `notes/`) импортированы через `cm import` →
**153 чанка**, оригиналы оставлены на месте, копии — в `imports/` (одноимённые `README.md` из
разных подпапок разведены хэш-суффиксами, рядом `*.meta.json` с origin). `recall` перепроверен
на двух запросах — релевантная гибридная выдача.

**Почему так, а не `cm init`.** `init` отказывается трогать существующую папку
(`{"status":"already_exists"}`, инвариант «никогда не клобберит стор», desc.md §5), поэтому
натравить его прямо на `memory/` нельзя. Развернул схему во временной папке тем же бинарником,
перенёс артефакты стора в `memory/` (не затрагивая существующие KB-`.md` и `notes/`), потом
импортировал содержимое штатным `cm import` по списку файлов. KB-проза в `notes/README.md` /
`notes/decisions.md` — обычные документы, поэтому пошла через `import` (chunks), а не через
`notes/<hex>.md`-формат заметок.

**Снят игнор `memory/`.** В корневом `.gitignore` была строка `memory` (вся папка вне git —
противоречило `CLAUDE.md`: «memory/ is meant to be committed»). Убрал только её (соседние
`tests`/`docs`/`scripts`/`CLAUDE.md` — намеренная локальная заглушка, не трогал). Теперь под git
попадает правда (исходные `.md`, `imports/`-копии + `.meta.json`, `config.json`), а внутренний
`memory/.gitignore` отсекает бинарник, `store.db` и `models/` — проверено `git add -n`.

**Осталось.** Папка ещё не закоммичена — ждёт явной команды. Стор живёт параллельно с KB:
правка `.md` руками требует `memory/cm.exe reindex`, чтобы индекс догнал файлы.

---

## 2026-06-18 — bulk-импорт `init` стал рекурсивным

**Что сделано.** `collect_md_files` в [../src/init.rs](../src/init.rs) теперь обходит target
**рекурсивно** (ручной DFS `walk_md`, без `walkdir` — инвариант минимума зависимостей), а не
только верхний уровень. Сигнатура сменилась на `collect_md_files(dir, exclude)`: второй аргумент —
только что созданная папка памяти (`folder`), её обход пропускается целиком, чтобы копии в
`imports/` не засосало обратно при `init ./` (когда `folder` лежит внутри `target`). Хелпер
`is_md` вынесен из замыкания (используется в обоих местах). Симлинк-директории не обходятся:
`entry.file_type()` из `read_dir` на всех ОС отражает сам линк, поэтому `is_dir()` для него ложен —
заодно защита от циклов/выхода за дерево доков. Промпт обновлён: «(включая вложенные папки)».

**Почему.** Типовой сценарий «папка с доками» (см. комментарий в `import_existing_md`) на деле
часто иерархичен: `docs/guide/intro.md`, `docs/api/*.md`. Старое поведение (только верхний
уровень) молча пропускало вложенное, и пользователь думал, что всё импортировано. Рекурсия
закрывает дыру; исключение папки памяти — чтобы рекурсия не создала петлю самопоглощения.

**Баг, пойманный живым тестом.** Первая версия исключала из обхода только *свою* новую папку
(`exclude`). Ручной прогон `init` второй раз в той же папке (другое имя) показал «Найдено 8»
вместо 4: рекурсия засосала `imports/`-копии *ранее созданной* папки памяти, для которой
`exclude` не совпадал. Это петля самопоглощения между двумя памятями. Починка: `walk_md` теперь
пропускает **любую** climem-папку памяти, опознавая её по маркеру `store.db` + `config.json`
(`is_memory_folder`), а не только `exclude`. Перепроверено живьём: повторный `init` снова видит
ровно 4 оригинала; look-alike-папка с одним лишь `config.json` (без `store.db`) маркером не
считается и её `.md` собираются. Также проверен non-interactive путь: при EOF в stdin импорт
тихо отклоняется (exit 0, оригиналы целы, store пуст), рекурсия при этом всё равно отрабатывает.

**Контракт синхронизирован.** По инварианту «contract & help в синке»: `help::HELP` (добавлена
строка про рекурсивный импорт при `init`), `README.md` (folder-of-docs shortcut: «или любой
подпапке, рекурсивно» + про пропуск папки памяти). Флаги/команды/формат вывода не менялись —
правок `cli::VALUE_FLAGS` не потребовалось. Тесты: переписан `collect_md_files_*` под новую
сигнатуру, добавлены `collect_md_files_recurses_into_subdirs` (сортировка — по полному пути,
детерминированно), `collect_md_files_prunes_excluded_folder` и
`collect_md_files_prunes_any_memory_folder_not_just_exclude` (регресс на пойманный баг). Весь
набор зелёный (305 тестов, clippy чист).

> Историческая правка: запись ниже (и заметка в этой же сессии про case-sensitive расширение)
> описывала `collect_md_files(target)` как «не рекурсивно» — это было верно на тот момент.

---

## 2026-06-18 — `help::HELP` переписан на английский под слабую LLM

**Что сделано.** Полностью переписал текст `cm help` ([../src/help.rs](../src/help.rs),
`HELP`) с русского на английский, переориентировав его с человека на модель-агента, причём на
самую слабую. Структура нового текста: блок «READ THIS FIRST» (6 главных правил: тело через
stdin, вывод JSONL, id = hex, recall/remember = главный цикл, ошибки самовосстанавливаются,
сначала `init`), таблица-навигатор «WHICH COMMAND DO I USE?» (задача → команда), затем для
каждой команды — назначение, точный синтаксис, copy-paste пример и **точная форма JSON-вывода**
(что печатает: `{"id":...}`, `{"found":false,...}`, `{"dangling":true,...}` и т.д.). Язык
буквальный, без идиом и метафор. Pointer (`help::pointer`) оставил русским — его вставляют в
человеко-читаемый `CLAUDE.md`.

**Почему.** Единственный реальный потребитель `cm help` — LLM в рантайме, а старый текст был
плотным русским «для разработчика»: метафоры («строка худая», «оживает»), без явного указания,
*какую* команду брать под задачу, и без форм вывода для парсинга. Слабая модель на таком тексте
ошибается выбором команды и парсингом. Новый формат снимает обе проблемы и дублирует
ключевые грабли (stdin/JSONL/hex-id) там, где они применяются.

**Контракт синхронизирован.** По инварианту «contract & help в синке» проверил против реального
поведения (`commands.rs`, `cli.rs`, `output.rs`): формы вывода, дефолты (`recall` limit 5,
`list` 20, `related` depth 1/limit 5, `log` 50), список `--fields`, порядок резолва store
(`--dir` → `MEMORY_DIR` → папка бинарника). Флаги/команды не менялись, поэтому правок
`cli::VALUE_FLAGS`/`README.md` не потребовалось. Тест `cli_help::help_command_prints_contract`
обновлён: ждёт английский заголовок `COMMANDS` вместо `КОМАНДЫ`. Решение и **правило «не
переводить обратно»** зафиксированы в [notes/decisions.md](notes/decisions.md) (раздел про
`help`). Все 302 теста зелёные, `clippy --all-targets` чист.

---

## 2026-06-18 — «хуманизация» комментариев в `src/` и `tests/`

**Что сделано.** Прошёлся по всем модулям `src/` (18 файлов) и интеграционным тестам `tests/`
и переписал комментарии так, чтобы они читались как написанные живым разработчиком, а не
сгенерированные. Конкретно убрал самые заметные «следы ИИ»: перебор длинных тире (—) и стрелок
(→/->) в прозе, формальные обороты («by construction», «load-bearing», «graceful handling»,
«first-class»), markdown-эмфазу (`**…**`, `*…*`) в doc-комментариях; тон сделал разговорнее,
кое-где короче. Заодно выправил «висячий» многострочный комментарий в `commands::remember`
(был отбит пробелами под trailing-комментарий — вынес на отдельные строки перед `let ids`).

**Почему так.** Запрос — применить «хуманизатор» к комментариям и коду. По уточнению:
интенсивность **сильная**, но технические перекрёстные ссылки (`desc.md §N`, `plan R1–R4/K1/
C1/E1/A2`, RRF, имена инвариантов) **сохранить** — это полезные указатели на источник правды и
часть конвенций ([conventions.md](conventions.md)). Язык комментариев оставил английским
(конвенция проекта). Контракт `help::HELP` и пользовательские строки/хинты не трогал, чтобы не
разойтись с README и тестами на `.msg.contains(...)`.

**Поведение не менялось** — это правки только текста комментариев. Единственная содержательная
правка ради чистоты линта: в `output.rs` добавил пустую doc-строку между списком и абзацем
(иначе `clippy::doc_lazy_continuation`). Проверки: `cargo clippy --all-targets` и
`--features api` чисты, все тесты зелёные (255 unit + интеграционные).

**Что осталось / на будущее.** `cargo fmt --all -- --check` показывает 7 **предсуществующих**
расхождений форматирования кода (свёртка коротких `AppError::with_hint`, перенос длинных
`eprintln!`/`log_op` в `commands.rs`/`init.rs`) — к комментариям отношения не имеют, специально
не трогал, чтобы не раздувать диф. Стоит прогнать `cargo fmt` отдельным коммитом.

---

## 2026-06-18 — переименование бинарника `memory` → `cm`

**Что сделано.** Инструмент переименован: продукт-бинарник теперь `cm` (`cm.exe` на Windows)
вместо `memory`. Один источник имени — `[[bin]] name` в `Cargo.toml` (`memory` → `cm`); сборка
даёт `target/release/cm.exe`. Дальше выровнено всё, что несёт имя бинарника или его как
запускаемую CLI-команду:
- `src/help.rs` — контракт (`HELP` + первая строка `cm — активная память`, секции `ВЫЗОВ`,
  `КОМАНДЫ`, `ПРИМЕРЫ`). `pointer()` не трогал — он подставляет реальный путь через `{exe}`.
- `src/init.rs` — имя самокопируемого бинарника (`cm.exe`/`cm`), строка `.gitignore`
  (`cm`/`cm.exe`), хинт `cm init`, тесты на `.gitignore`/хинт.
- Все хинты ошибок (`AppError::with_hint`) в `commands.rs`, `config.rs`, `import.rs`,
  `export.rs`, `embed/api.rs`, `embed/mod.rs`, `main.rs` (`cm help`, `cm reindex`, …).
- `cli.rs` doc-комментарий; тесты `tests/common/mod.rs`, `tests/api_provider.rs`
  (`cargo_bin("cm")`), `tests/cli_help.rs` (ассерт на `cm recall` / `cm help`).
- Документация: `README.md` (заголовок + весь контракт), `CLAUDE.md`, `desc.md`, вся база
  знаний `memory/` (overview/structure/build/conventions/known-issues/roadmap/notes/howto),
  `docs/*`, `TEST_PLAN.md`, `scripts/bench.py` (`find_binary` ищет `cm.exe`/`cm`; бенч-запрос
  про «имя бинарника» обновлён на `cm`).

**Почему.** `cm` короче и быстрее набирается — это инструмент, который модель/человек дёргают
часто из шелла. Имя binary — единственный публичный контракт (так задумано: контракт живёт
*внутри* бинарника, см. `decisions.md`), поэтому правка точечная и согласованная: `help::HELP` +
`cli::VALUE_FLAGS` + README в одном изменении (инвариант «контракт и help в синхроне»).

**Что НЕ трогал и почему.** Имя — это только бинарник. Осталось как было:
- крейт `climem` (имя продукта-как-пакета, не команды);
- env-переменная `MEMORY_DIR` (контракт «где хранилище», не имя бинарника — переименование
  ломало бы существующие окружения без выгоды);
- дефолтная папка-хранилище `.memory` и пример `--name project-memory`;
- база знаний `memory/` (это папка с доками репо, путь, а не команда);
- концепт-слова «активная память / память / memory tool» и имя дамп-файла `memory-dump.md`.
- **История журнала.** Старые записи (`memory.exe` в записях ниже) оставлены как есть — журнал
  это лог «что было», а не текущее состояние; перезаписывать прошлое = подделывать запись.

**Проверка.** `cargo build --release` → `cm.exe`; `cm.exe help` печатает контракт от `cm`.
`cargo test --release` — зелёно (255 unit + интеграционные, 0 падений); `cargo clippy
--all-targets` чисто. Grep по `memory.exe` / `memory <subcommand>` / `cargo_bin("memory")` по
`src`+`tests` пуст; по docs — только легитимные `MEMORY_DIR`/`.memory`/`memory/`/`memory-dump.md`.

---

## 2026-06-18 — интерактивный импорт при `init` ("docs folder" режим)

**Что сделано.** `init` теперь определяет, есть ли `.md` файлы в target-директории, и если да —
предлагает их импортировать интерактивно. Логика в `src/init.rs`:
- `collect_md_files(target)` — сканирует target (не рекурсивно), возвращает отсортированный
  список `.md`-файлов.
- `prompt_yes_no(question)` — печатает вопрос в stderr, читает строку из stdin; `true` для
  `y/yes/д/да`; `false` при EOF/ошибке (пайп → тихий пропуск, не ломает скрипты).
- `import_existing_md(...)` — если пользователь ответил `y`: строит эмбеддер, импортирует
  каждый файл через `import::import_file`, выводит JSONL на stdout. Потом спрашивает про
  удаление оригиналов (только если хоть один успешно импортирован).

**Почему.** Сценарий: пользователь кладёт `memory.exe` в папку с документами, запускает
`memory init ./` — прога видит `.md` файлы и предлагает их всосать сразу. Без этого пришлось бы
вызывать `import` по одному.

**Инварианты сохранены.** stdin для интерактивного ввода — только в `init` (не конфликтует с
`read_stdin` в `remember`, который читает тело заметки из stdin в других командах). Импорт идёт
через стандартный `import::import_file` — идемпотентен, копирует оригинал в `imports/` до
индексации. JSONL на stdout, нарратив в stderr.

**Само-ревью + ручное тестирование (5 сценариев Bash + PowerShell).** Нашёл и починил два бага:
- **BOM в пайпе под PowerShell.** `"y" | memory init` НЕ срабатывал — PS добавляет BOM, и
  `"\u{feff}y" != "y"`. Та же грабля, что чинит `read_stdin` (conventions.md). Вынес парсинг в
  чистую `is_yes()`, которая стрипит BOM перед сравнением. Интерактивный ввод (печать y+Enter)
  BOM не имеет — это артефакт редиректа, так что основной путь и так работал; фикс — для пайпа.
- **Case-sensitive расширение.** `collect_md_files` матчил только строчное `.md`, а `import`
  принимает `.md`/`.markdown` case-insensitively → `README.MD` молча пропускался. Выровнял:
  скан теперь lowercase'ит расширение и принимает оба, совпадая с тем, что реально умеет импорт.

Добавлены юнит-тесты на чистые хелперы (`is_yes`, `collect_md_files`); README дополнен описанием
режима «папка с доками». Все 255 unit + интеграционные тесты зелёные, clippy чист.

---

## 2026-06-17 — тестирование CLI + фикс BOM в read_stdin (Windows PowerShell)

**Что сделано.** Прогнал полный smoke-тест `memory.exe`: `init`, `remember` (stdin), `recall`
(гибридный поиск, `--explain`, `--fields`, `--tag`), `get`, `list`, `related` (вики-ссылки
как dangling), `forget`, `import` (README.md → 23 чанка), `reindex` (`--all`), `export md`,
`log`, `log --imports`, `config`. Всё работает.

**Баг найден и исправлен.** `read_stdin()` в `commands.rs` не обрабатывал BOM.
PowerShell 5.1 при `$OutputEncoding = [System.Text.Encoding]::UTF8` пайпит **два** BOM
(EF BB BF EF BB BF): один — preamble от `$OutputEncoding`, второй — на каждую строку
(поведение PS 5.1). Без `$OutputEncoding` вообще шлёт UTF-16 LE и кириллица становится `???`.
Фикс: `s.trim_start_matches('\u{feff}')` снимает любое количество ведущих BOM-ов.
`strip_prefix` не подошёл — снимает ровно один.

**Что осталось.** Заметки, записанные до фикса через PS без правильной кодировки, содержат
`???` в теле — их надо удалить или перезаписать вручную. Корень проблемы не в бинарнике —
в Windows-специфике. Документировать в `conventions.md` (`$OutputEncoding`-workaround).

---

## 2026-06-17 — смена концепции хранения: md = правда, store.db = производный индекс + граф знаний

**Что сделано.** Реализована задумка [`desc.md §3`](../desc.md) (она существовала только на бумаге —
код хранил всё в `store.db` как в единственном источнике правды). Теперь **источник правды — md**:
`notes/<id>.md` (1 файл = 1 заметка, ручной `---`-frontmatter + тело) и `imports/` (оригиналы +
`<name>.meta.json`-сайдкары с orig-именем и тегами). `store.db` — **производный, пересобираемый**
индекс (FTS5 + векторы + граф). По фазам:
- **id → hex-строка.** Атомарный флип `i64 → String` по всему коду (`NoteRow.id`, get/forget/list/
  all/ids_matching/all_embeddings/fts_search, output, ранжирование в `search`, `parse_id` теперь
  валидирует `^[0-9a-f]+$`). id минтится (`store::fresh_id`/`mint_hex`, FNV-фолд time+pid+counter,
  collision-check), `id == notes/<id>.md`. FTS5 держит внутренний INTEGER rowid; `fts_search`
  джойнит `notes` по rowid и отдаёт публичный hex; `upsert_note` — явный DELETE+INSERT (rowid не виснет).
- **note.rs** — ручной парсер/рендер frontmatter (без serde_yaml), байт-стабильный фикс-порядок ключей.
- **remember** пишет `notes/<id>.md` (**точка коммита**), затем индексирует best-effort (сбой
  эмбеддера → предупреждение в stderr, заметка цела, `{"id"}` печатается), пишет sync, выводит рёбра
  из `[[вики-ссылок]]` тела. **remember больше не падает на сломанном эмбеддере.**
- **forget** удаляет md + строку + рёбра; **kind-aware**: чанк импорта удалить нельзя (подсказка).
- **import** копирует оригинал в `imports/` + сайдкар (truth) ДО индексации; чанки — производные,
  **контент-адресные** id `content_hash(file_hash#n)`, `source` = копия в imports/. Идемпотентно.
- **reindex [--all]** — пересборка из notes/+imports/. Инкрементально по `content_hash` (таблица
  `sync`); `--all` → `wipe_derived` (стирает notes/fts/sync/imports/edges, НЕ oplog/meta) + переэмбед.
  Двухпроходно (сбор slug→id, затем рёбра — forward-ссылки сходятся). Прунинг исчезнувших файлов.
  **Legacy-guard**: `--all` отказывается стирать, если в БД есть заметки, а `notes/` пуст. Деградирует
  до FTS-only без эмбеддера.
- **граф знаний** (`graph.rs` + таблица `edges`): рёбра из frontmatter `relations` и `[[вики-ссылок]]`
  (`links_to`). `related <id>` — детерминированный BFS, «худой» JSONL, висячие цели первым классом.
- **init** разворачивает notes/+imports/ + пишет `.gitignore` в папку памяти (игнорит производную
  store.db). `config.version` 1 → 2.

**Почему так (ключевые решения).**
- **id = случайный hex во frontmatter, а не autoincrement.** БД производна — счётчик БД не может быть
  идентичностью (ребилд перенумеровал бы и сломал get/forget/связи). desc.md §3 буквально показывает
  `id: 0a1b2c`. Цена — широкий ripple i64→String, принят как атомарный коммит (тип-система неделима).
- **md — точка коммита, индекс — best-effort.** desc.md §3/§10: запись файла — коммит, индекс
  пересобираем. Поэтому remember пишет md ДО сборки эмбеддера и не падает на его сбое (раньше падал).
- **imports/ самоописателен (сайдкары).** Теги импорта не выводятся из файла; держим их в
  `imports/<name>.meta.json` (truth), а не только в БД — тогда **удаление store.db не теряет теги**,
  reindex восстанавливает и контент, и метаданные (иначе — дыра в §10).
- **Разрешение цели графа — slug по умолчанию, префикс `id:` форсирует id.** Никакой эвристики «похоже
  на hex» (8-hex slug мог бы перехватить адресацию) — убрал целый класс ложных срабатываний.
  Висячее ребро — первоклассно (dst_id NULL, dst_raw хранится), оживает на reindex.
- **edges/imports — производные, выводятся из md; oplog/meta переживают wipe** (их из файлов не
  собрать). Всё, что знает индекс/граф, выразимо в md+сайдкарах — иначе БД перестаёт быть одноразовой.
- **Граф — отдельный глагол `related`, не в `recall`.** Сохранил неизменным нагруженный путь
  ранжирования RRF (детерминизм/`score==fts+vector`); RRF-канал графа спроектирован, но отложен.

**Процесс.** Перед кодом — два состязательных воркфлоу (анализ влияния миграции; дизайн графа) с
критиком: нашли блокер (legacy-данные при `--all`), идентичность/forget чанков, не-атомарность
флипа id, FTS-rowid↔hex — все учтены. Сборка велась фазами с зелёными тестами между ними.

**Состояние.** `cargo test` зелено: **249 in-module + 45 e2e** (default / `--no-default-features` /
`--all-features`); `clippy --all-targets -D warnings` чисто; `fmt` чисто; релиз + `--features pdf`
собираются. `help::HELP`, `README.md`, `desc.md §5/§6` синхронны контракту.

**Доделано (та же сессия).** Отложенные доработки из карты закрыты: (1) **граф-канал в
`recall --related <id>`** — третий RRF-канал (`score = fts+vector+graph`, `search::recall_with` +
`neighbor_ranks`), вес `search.hybrid_weights.graph` по умолчанию 0.0 (канал выкл → plain `recall`
байт-в-байт прежний); (2) **флаги `remember --slug`/`--relations`** (`parse_relations`, во frontmatter
+ рёбра сразу, `set_note_slug`); (3) **warning о коллизии slug** на `reindex` (`graph::slug_collisions`).
После доработок: **252 in-module + 47 e2e** зелено (default / `--no-default-features` / `--all-features`),
clippy `-D warnings` + fmt чисто. См. [roadmap.md](roadmap.md) «Доделано после миграции».

**Осталось (by design / отдельные истории).** Упрощения, сознательно не трогавшиеся: `[[ссылки]]` в
код-блоках считаются за рёбра; порядок `list`/`all` после reindex = порядок чтения каталога (не исходный
`created`); граф-свежесть — висячее ребро на новую цель оживает на следующем `reindex`; `export md`
без round-trip. Крупные точки расширения (MCP-сервер, ONNX-провайдер, ANN-индекс, token-Phase-4 X1–X5)
и утечка секрета в `config get/set` — в [roadmap.md](roadmap.md)/[known-issues.md](known-issues.md).

---

## 2026-06-18 — реализован план токен-эффективности, Фазы 0–3 ([docs/token-efficiency-plan.md](../docs/token-efficiency-plan.md))

**Что сделано.** Второй порядок экономии токенов на `recall` (главный выигрыш — retrieval — уже
был). По фазам плана:
- **Фаза 0 — мерило.** [scripts/bench.py](../scripts/bench.py): импортирует `memory/`, гоняет
  канонические запросы, считает токены (`tiktoken cl100k_base`) lean vs `--explain` и проверяет
  **recall@k-страж** (ожидаемый `origin` в top-k). На текущей KB: recall@5 = 8/8, lean ≈ на
  7.6 % меньше `--explain` (это только дельта тройки чисел; основной E1/C1-выигрыш — против
  старого формата). Не часть `cargo test`.
- **Фаза 1 (E1, C1, A2).** `recall` по умолчанию печатает «худую» строку (`output::recall_value`):
  `id, kind, body` + непустые `tags`/`origin`/`source`; `score/fts/vector` — под `--explain`;
  `--fields a,b,c` — whitelist `RECALL_FIELDS`. Дефолт чанка 512/64 → **200/32 слов** (`max_tokens`
  переосмыслён как слова, имя сохранено ради совместимости). Ключи JSON — алфавитно/байт-стабильно.
- **Фаза 2 (R1–R4).** RRF вместо min-max (`search::recall_with`, `score = Σ w/(k+rank)`,
  `k=search.rrf_k=60`); глубокий лимит-независимый пул FTS (`search.candidates=50`); полный
  breadcrumb заголовков в `chunk::markdown` (`origin` = `файл › H1 › H2 › H3` + префикс предков
  в тело, бюджет в словах); pre-filter `--tag`/`--origin-prefix` (`store::ids_matching`).
- **Фаза 3 (K1).** `--min-score` (порог в RRF-шкале) + дефолт `recall --limit` 8 → **5**.

**Почему так (ключевые решения).**
- **RRF, не нормированная min-max** (решение — [notes/decisions.md](notes/decisions.md)). Сливает
  *позиции*, а не несопоставимые bm25/cosine → стабильный top-k под безопасную k-резку. Цена:
  отчётные `score/fts/vector` стали мелкими (`~w/(k+rank)`, не [0,1]); `score == fts+vector`.
  Заодно унифицировал кандидатов на `BTreeSet` + tie-break по `id` → **закрыл баг tie-order**
  ([known-issues.md](known-issues.md)).
- **Сигнатуру `search::recall` не ломал**: добавил `recall_with(.., &RecallOpts)`, а `recall(.., limit)`
  оставил тонкой обёрткой — `export` и ~15 unit-тестов не тронуты.
- **Lean by default, полнота по запросу.** Контракт остался JSONL; полная запись — `get <id>`;
  отладка — `--explain`. Срезает «конверт» без потери качества выдачи.
- **`max_tokens` не переименовывал** — сломало бы существующие конфиги; задокументировал как
  «слова» в коде и [conventions.md](conventions.md), снизил дефолт.
- **Фаза 4 (X1–X5) не делал** — план сам помечает её opt-in/по росту KB; X4/X5 вдобавок
  `api`-only. Зафиксирована в [roadmap.md](roadmap.md).

**Отклонения/находки.** `round4` вынесен в `output.rs` (используется проекцией) — возвращает
*расширенный f32* f64, поэтому в юнит-тестах сравнение с допуском, не точное JSON-равенство.
Удалены `norm01`/`min_max*` и их тесты (RRF их не использует). Два chunk-теста и два search-теста
переписаны под новое поведение. `cargo test` (default + `--no-default-features`), `clippy
--all-targets --all-features -D warnings`, `cargo fmt` — зелено.

**Что осталось.** Фаза 4 (preview-then-get, extractive-эксцерпт, дедуп при импорте; reranker/
RAPTOR за `api`). Два regression-anchor'а в [known-issues.md](known-issues.md) (утечка маскирования,
частичный импорт) — по-прежнему открыты, вне этой задачи.

---

## 2026-06-17 — реализован план тестирования ([docs/testing-plan.md](../docs/testing-plan.md))

**Что сделано.** С нуля заведено тестовое покрытие. ~204 unit/integration через
`#[cfg(test)] mod tests` **внутри каждого `src/*.rs`** + ~39 e2e в `tests/` через `assert_cmd`
(чёрный ящик). Оснастка — `tests/common/mod.rs`, фикстуры — `tests/fixtures/`. Провайдер `api`
покрыт против `httpmock` (`tests/api_provider.rs`, под `required-features = ["api"]`). Dev-депы:
`tempfile`/`assert_cmd`/`predicates`/`httpmock`. Зелено на `default`/`--no-default-features`/
`--all-features`; `clippy -D warnings` чисто. Детали раскладки — [structure.md](structure.md)
«Тесты», команды — [build.md](build.md) «Тесты».

**Почему так (ключевые решения).**
- **Без `src/lib.rs`** (как и предлагал план §3.2). Crate binary-only; внутреннюю логику
  (приватные парсеры, нормировка, хеширование, `Store`, `recall`, `import_file`) видят только
  in-module unit'ы. Контракт (argv/stdin/JSONL/exit) — e2e через процесс. `lib.rs` потребовал
  бы рефактора раскладки без соразмерной пользы.
- **`Store::open(":memory:")`** для unit'ов store/search/import — изолированная in-memory БД без
  правок кода; персистентность/`create` — только на temp-файле (`tempfile`).
- **Кросс-платформенность e2e:** позиционные пути импорта — **относительные** (ведущий `/`
  парсится как Windows-флаг — см. [conventions.md](conventions.md)); добавлен
  `common::run_cwd`. Мок `api` — только `http://` (ureq+native-tls).
- **Детерминизм:** числа — с допуском (вывод через `round4`, плюс артефакт расширения f32→f64);
  время — по формату, не значению; порядок `recall` при равных score не ассертится.

**Отклонения от плана (план местами расходился с кодом — пинали фактическое поведение).**
- `markdown()` **не делегирует** в `text()` на непустом входе без заголовков: получается одна
  секция с пустым title → origin `""` (а не `#N`, как ждал план).
- `config get/set` секрета **не маскирует** лист → утечка; план ждал `***`. Зафиксировано как
  regression-anchor ([known-issues.md](known-issues.md)).
- `httpmock` нельзя гейтить фичей в `[dev-dependencies]` (Cargo не поддерживает
  `target.'cfg(feature=…)'`); вместо этого цель `api_provider` под `required-features`.
- **Мелкие правки в проде** (не поведение): `#[derive(Debug)]` на `ImportResult`; 4 тривиальных
  clippy-лита (`parse_limit`, `Search` derive `Default`, `new_named` struct-update, лишний
  каст в `civil_from_days`) — чтобы `clippy -D warnings` был чист. Инвариант весов 0.5/0.5
  сохранён.

**Что осталось / на будущее.** PDF happy-path (`import_pdf_enabled_extracts_text`) пропущен —
нужен валидный `sample.pdf` (покрыта только ветка-ошибка). Три regression-anchor'а в
[known-issues.md](known-issues.md) ждут решения (утечка маскирования, частичный импорт,
tie-order `recall`). CI-матрицу (фичи × {ubuntu, windows}) можно поднять, когда проект встанет
под git.

---

## 2026-06-17 — заведена память проекта

**Что сделано.** Создана папка `memory/` — система активной (для людей/модели) памяти по
образцу [HOWTO-BUILD-MEMORY.md](HOWTO-BUILD-MEMORY.md) из соседнего проекта ccDoc. Наполнены
ядровые файлы фактами из кода: [overview.md](overview.md), [structure.md](structure.md),
[build.md](build.md), [conventions.md](conventions.md), [notes/decisions.md](notes/decisions.md),
рецепты в [howto/](howto/), регламент [RULES.md](RULES.md) и правила работы над кодом в
[rules/](rules/).

**Почему так (ключевые адаптации под climem).**
- climem — **одиночный Rust-бинарник**, а не монорепо, как ccDoc. Поэтому: нет `deploy.md`
  (серверного деплоя нет — «дистрибуция» это self-copy через `init`, описана в build.md);
  `structure.md` — карта модулей `src/` и схемы SQLite, а не пакетов/IPC.
- `analysis/` и часть подпапок **не заведены про запас** — по правилу роста «файл заводится,
  когда есть что положить» ([RULES.md](RULES.md) §5). Появятся по событию.
- Контент — на русском (язык `desc.md`/`README.md` проекта); комментарии в коде остаются
  английскими, как уже принято в `src/`.

**Состояние проекта на момент записи.** Ядро контракта реализовано, `cargo build --release`
зелёный, бинарник `target/release/memory.exe` собран. Проект ещё **не под git**. Известных
багов нет ([known-issues.md](known-issues.md) пуст).

**Что осталось / на будущее.** Точки расширения зафиксированы в [roadmap.md](roadmap.md)
(MCP-режим, локальная нейромодель ONNX, PDF за фичей). Когда проект встанет под git — стоит
добавить Stop-хук-напоминание об обновлении памяти ([RULES.md](RULES.md) §7).

---

## 2026-06-18 — Анализ графа знаний (без правок кода)

**Что.** Многоагентный разбор построения и обхода графа знаний (6 измерений, 45 findings,
39 подтверждены состязательной верификацией + эмпирически через собранный `cm.exe`). Результат —
[../docs/graph-analysis.md](../docs/graph-analysis.md): сильные стороны, проблемы (4 бага, 11
дизайн-провалов, 4 doc-mismatch, 4 ограничения), приоритизированный план P0–P2. Баги заведены в
[known-issues.md](known-issues.md) (B1–B4).

**Почему важно.** Граф формально производен от md и в тракте `reindex --all` корректен, НО
инкрементальный тракт нарушает заявленные инварианты: **(f)** «висячее ребро оживает на reindex» не
работает без `--all` (B1); рёбра синхронизируются только по `src_id`, поэтому удаление цели
оставляет осиротевшие резолвленные рёбра и теряет связь из вывода (B2/B3), нарушая **(d)**. Всё
воспроизведено вживую (graph-analysis.md §5). Общий корень — переиндексация только `changed_notes`
во втором проходе + односторонняя синхронизация (`delete_edges_from` без `delete_edges_to`).

**Решений по коду не принято** — только анализ. Реализация по плану P0 потребует новых инвариантов
в [notes/decisions.md](notes/decisions.md): «удаление/прунинг трогает входящие рёбра» и
«инкрементальный reindex оживляет висячие на новые slug/id».
