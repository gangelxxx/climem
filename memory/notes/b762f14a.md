---
id: b762f14a
created: 2026-06-19T14:10:27Z
tags: review,bugfix,cleanup
---
Fixed all 9 low-severity findings from the 2026-06-19 code-review-skill audit (the 2 high ones were fixed earlier). All clippy-clean (api,code features), fmt-clean, 329+ tests green:

- chunk.rs markdown(): mem::take/move instead of clone() on section flush (no multi-KB body clone per heading).
- config.rs load()/load_raw(): match on io::ErrorKind so only NotFound shows the "run cm init" hint; permission/IO/is-a-directory errors surface the real message (was map_err(|_| ...) lying "not found").
- config.rs mask_secrets(): now redacts URL userinfo in any string value (e.g. embedding.endpoint https://user:pass@host -> https://***@host); verified via cm config get. New helper redact_url_userinfo + test.
- search.rs recall_with(): cosine sort uses total_cmp (was partial_cmp().unwrap_or(Equal) -> non-total order if a stored vector has NaN). Also dropped hot-path id clones (embeds.into_iter, ids consumed by value).
- search.rs recall(): the limit-only wrapper is now #[cfg(test)] (only tests use it after export switched to recall_with) to stay dead-code-clean.
- output.rs print_line(): writeln! to locked stdout, exit(0) on BrokenPipe (was println! -> panic on `... | head`). Verified clean exit.
- commands.rs export(): --query now uses recall_with with the same pre-filters as recall (--tag/--origin-prefix/--min-score/--limit), no longer diverging. Contract synced: help::HELP + README export rows updated; flags already in cli::VALUE_FLAGS.
- commands.rs recall(): stderr warning when --explain is passed with --fields (it was a silent no-op).
- embed/api.rs request(): reject a response whose length != configured embedding.dimension (was unbounded; could bloat store.db). dim is now read so removed its #[allow(dead_code)].

Issue write-ups remain under docs/issues/ (gitignored). README/help/cli kept in sync per project invariant.
